[package]
name = "fingest-kernel"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
description = "Shared kernel: pure domain value objects, errors, clock and event ports. No I/O."

# Dependency rule: this crate MUST NOT depend on sqlx, actix-web, tokio or any adapter crate.
# Enforced by tests/dependency_rule.rs.
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }
bigdecimal = { workspace = true }
thiserror = { workspace = true }
async-trait = { workspace = true }

[lints]
workspace = true
