[package]
name = "fingest-wallets-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Wallets bounded context: wallets, expenses and balance invariants. No I/O."

# Dependency rule: interior of the hexagon. No sqlx, actix, tokio.
[dependencies]
fingest-kernel = { path = "../fingest-kernel" }
serde = { workspace = true }
chrono = { workspace = true }
bigdecimal = { workspace = true }
thiserror = { workspace = true }
async-trait = { workspace = true }

[features]
test-support = []

[dev-dependencies]
futures-executor = { workspace = true }
serde_json = { workspace = true }

[lints]
workspace = true
